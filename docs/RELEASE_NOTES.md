## 下载

直接下载 `TGReactionRanker.exe` 运行即可。

## 这个版本修复了什么

- 修复矩阵页下滑时可能突然黑屏或出现空白的问题。
- 矩阵页从固定分屏虚拟列表改为连续瀑布流追加加载。
- 改进 `花花🌸主播直播` 这类没有公开频道 handle 的导出链接跳转。
- 没有公开消息直链时，会优先使用当前消息里的 Telegram 链接，再退回导出里的频道入口。

## 使用提醒

Telegram 导出文件不一定包含公开消息直链。软件会尽量自动识别，但不会把消息正文里其他频道的广告链接强行当成当前频道链接。
